- const ROWS = 7
- const CIRCLES = 14
- let r = 0
while r < ROWS
  .row(style=`--count: ${CIRCLES}; --delay: ${r};`)
    - let c = 0
    while c < CIRCLES
      .row__circle.circle(style=`--index: ${c};`)
      - c++
  - r++