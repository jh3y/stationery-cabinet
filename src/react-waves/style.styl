.waveContainer {
  position: absolute;
  left: 0;
  right: 0;
  height: 100vh;
  overflow: hidden;
}

.waveContainerBottom {
  bottom: -1%;
}

.waveContainerTop {
  top: -1%;
  transform: rotate(180deg);
}

.wave {
  animation: wave calc(var(--speed, 0) * 1s) infinite linear;
  background-size: 50% 100%;
  bottom: -5%;
  height: calc(var(--height, 0) * 1vh);
  left: 0;
  opacity: var(--opacity);
  position: absolute;
  right: 0;
  width: calc(var(--width, 0) * 1vw);
  overflow: visible;
}
.wave path {
  fill: hsl(
    var(--hue),
    calc(var(--saturation, 0) * 1%),
    calc(var(--lightness, 100) * 1%)
  );
}
@keyframes wave {
  to {
    transform: translate(-50%, 0);
  }
}
